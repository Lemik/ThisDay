/**
 * Copy to config.js (optional).
 * Loaded before app.js. Default coordinates when geolocation is denied or unavailable.
 */
window.THISDAY_CONFIG = {
  defaultLatitude: 40.7128,
  defaultLongitude: -74.006,
};
